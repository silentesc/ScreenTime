/**
 * Called when a new process is stopped
 * Arguments:
 *   path: String - The .exe path of the process that was stopped
 * Returns:
 *   None
 */
pub fn on_process_stop(_path: String) {
    // println!("[-] {}", path);
}
